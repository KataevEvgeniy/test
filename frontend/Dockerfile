FROM node:18-alpine

# Устанавливаем рабочую директорию
WORKDIR /app

# Копируем статику внутрь контейнера
COPY . .

# Устанавливаем http-сервер
RUN npm install -g serve

# Указываем порт, на котором будет работать сервер
EXPOSE 3000

# Команда запуска
CMD ["serve", "-s", ".", "-l", "3000"]